services:
  - type: web
    name: ai-facebook-factory
    runtime: node
    plan: free

    buildCommand: |
      npm install

    startCommand: npm start

    envVars:
      - key: NODE_VERSION
        value: 24
